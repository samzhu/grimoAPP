@AGENTS.md

## Claude Code

- 前端 UI / UX / layout / onboarding / empty state / error state / visual regression 工作，先讀 `docs/grimo/design/README.md`，再依索引讀需要的設計文件。
- `CLAUDE.md` 只放跨 session 必須知道的入口和規則；詳細設計決策寫進 `docs/grimo/design/*` 或 active spec，不在這裡重複。
