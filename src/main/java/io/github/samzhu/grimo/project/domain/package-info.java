/**
 * Project domain types — exposed as part of project::api so that
 * cross-module consumers of {@code ProjectUseCase} can access
 * return types ({@code Project}, etc.).
 */
@NamedInterface("api")
package io.github.samzhu.grimo.project.domain;

import org.springframework.modulith.NamedInterface;
