/**
 * Public API of the Session module — exposed to other modules via
 * {@code allowedDependencies = { "session::api" }}.
 */
@NamedInterface("api")
package io.github.samzhu.grimo.session.application.port.in;

import org.springframework.modulith.NamedInterface;
